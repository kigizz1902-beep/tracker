# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code(claude.ai/code)에게 제공되는 안내 문서입니다.

## 프로젝트 현황

이 저장소에는 현재 PRD(`docs/PRD.md`)만 존재하며, 실제 애플리케이션 코드나 `package.json`, 빌드 도구는 아직 스캐폴딩되지 않았습니다. 프로젝트가 초기화되기 전까지는 실행할 빌드/린트/테스트 명령어가 없습니다. 스캐폴딩이 시작되면 실제 명령어(개발 서버 실행, 빌드, 린트, 테스트 — 단일 테스트 실행 방법 포함)로 이 파일을 업데이트하세요.

## 이 프로젝트는 무엇인가

책/영화를 다 본 뒤 제목, 평점, 한줄평, 완료일을 기록하고 카드형 갤러리에서 조회하는 1인용 웹앱입니다.

**이 프로젝트는 `docs/PRD.md`를 기준으로 개발합니다. 새 기능을 만들 때는 항상 먼저 PRD.md를 참고하세요.**

## 개발 원칙

- **개발 순서**: 화면을 가짜(Mock) 데이터로 먼저 완성한다 → 실제 Notion 데이터를 연결한다 → 예외 처리(네트워크 오류, 입력 유효성 검사 등)는 마지막에 추가한다. 이 순서를 앞당기지 않는다.
- **개발 중 데이터 연동**: 개발 중에는 연결된 Notion 도구를 그대로 사용해 기능을 구현하고 테스트한다.
- **배포 전환 필수**: 이 연결 방식은 배포된 뒤에는 그대로 쓸 수 없다. 배포 준비 단계가 되면 안전한 방식(서버 쪽 코드 + 환경변수)으로 반드시 바꿔야 하며, 그 전까지는 이 부분(서버 쪽 보안 처리)을 미리 구현하지 않는다.
- **인증 정보 취급**: 토큰, 키 등 인증 정보는 어떤 경우에도 화면(프론트엔드) 코드에 직접 넣지 않는다.
- **커밋 단위**: 아래 기능 단위 작업이 끝날 때마다 커밋한다 — 초기 세팅 / 화면 뼈대 / Notion 연동 / CRUD 완성.
- **애매한 부분은 먼저 질문**: 코드를 짜기 전에 요구사항이 애매하면 임의로 결정하지 말고 항상 먼저 사용자에게 물어본다.

## 핵심 아키텍처 결정사항 (PRD 기준 — PRD를 함께 업데이트하지 않고는 벗어나지 말 것)

- **데이터 저장소: Notion 단일 소스 (고정 결정).** Google Sheets는 이 프로젝트의 데이터 소스로 쓰지 않습니다. PRD나 이후 요청에 Sheets/스프레드시트 관련 내용이 다시 언급되더라도, 사용자가 명시적으로 재도입을 결정하기 전까지는 **무시하고 Notion 단일 소스로만 구현**하세요 — Sheets 연동 코드나 이를 위한 데이터소스 추상화 레이어를 만들지 마세요. Notion 데이터베이스("독서영화트래커")는 이미 아래 속성/타입으로 생성되어 있으며, 이 DB에 쓰는 모든 코드는 이 스키마와 정확히 일치해야 합니다:
  - 제목 (title)
  - 유형 (select: 책, 영화)
  - 평점 (number)
  - 한줄평 (text)
  - 완료일 (date)
  - 상태 (select: 읽는 중, 완료, 보류)
  - 커버이미지 (url)
- **스택: React + Vite, Tailwind CSS, lucide-react(아이콘).** 상태 관리는 `useState`/`useContext`만 사용하며, Redux/Zustand 등 별도 상태관리 라이브러리는 도입하지 않습니다. Notion 접근은 공식 `@notionhq/client` SDK를 사용합니다.
- **Notion 토큰 처리는 현재 설계상 개발 단계 전용입니다.** 개발 중에는 연결된 Notion 도구를 직접 사용하며 별도의 서버 쪽 보안 처리를 미리 만들지 않습니다. Vercel 배포 전에는 반드시 서버 쪽 코드(예: Vercel Serverless Functions) + 환경변수 방식으로 전환해 토큰이 클라이언트에 노출되지 않도록 해야 합니다 — PRD는 이를 배포 시점에 결정할 사항으로 명시적으로 남겨두었습니다.
- **커버이미지 자동 검색 (확정 기능).** 책은 Open Library Search API(인증키 불필요 — 프론트엔드에서 직접 호출 가능), 영화는 TMDB Search API(API 키 필요) 사용. TMDB 키는 Notion 토큰과 동일한 원칙 적용 — 반드시 서버 쪽 코드(Vercel Serverless Function)를 통해서만 호출하고, 키는 환경변수로 관리하며 프론트엔드 코드에는 절대 노출하지 않는다. 후보가 여러 개면 선택 UI를 제공하고, 후보가 없거나 API 호출이 실패해도 기존 커버이미지 URL 직접 입력 필드는 그대로 유지한다.
- **명시적 Non-goals**: 로그인/인증 없음, 다중 사용자 지원 없음, 실시간 동기화/공유 없음.

## PRD가 의도적으로 미해결로 남겨둔 사항 (Open Questions)

**아래 항목들은 아직 확정되지 않았습니다. 임의로 결정하지 말고, 구현 시 이 항목과 관련된 코드를 짜야 한다면 먼저 사용자에게 확인하세요.**

- 기본 정렬 순서(완료일순 vs 평점순)
- 삭제 확인 UX(모달 확인 vs 삭제 후 되돌리기)
- 평점 범위/형식(1~5 vs 1~10, 소수점 허용 여부)
- 완료일이 없는 기록(예: "읽는 중" 상태)의 정렬/표시 방식
- 커버이미지 URL이 깨졌을 때의 대체 처리
- 기록이 많아질 경우의 페이지네이션/무한스크롤 여부
- 프로덕션에서 Notion 토큰을 보호하는 구체적 방법(서버리스 함수 경유는 결정됨, 캐싱/레이트리밋 세부사항은 미결정)
- 커버이미지 자동 검색 후보 개수, 동명 영화/도서 구분 표시 방법
- TMDB API 키 보유 여부(구현 착수 전 확인 필요), 검색 API 실패 시 UX
